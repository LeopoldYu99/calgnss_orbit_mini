# 定轨算法接口（纯 C 单文件版）

原讨论方案已实现，库实现已合并到 `src/orbit_determination.c`，不依赖 C++。正式定义以 [orbit_determination.h](include/orbit_determination.h) 为准，构建和行为说明见 [README.md](README.md)。

本次确定的接口变化：

- `config.observation_capacity` 设置环形位置观测缓存点数，默认 30，满后淘汰最旧点。
- `config.fit_degree` 设置拟合阶数，默认 3；必须小于缓存容量。
- 原草案的 `od_get_latest_state` 已改名为 `od_get_orbit`。
- 保留 `od_feed_rtcm` 和 `od_feed_nmea` 两个流式输入接口。
- 输出最新有效位置观测历元的 J2000 位置、速度及 UTC 毫秒时间戳。
- 不提供历史查询接口或未来外推接口，外推算法源码与专用缓存已从独立库移除。

```c
od_config_t config = od_default_config();
config.observation_capacity = 30;
config.fit_degree = 3;

od_status_t od_create(const od_config_t *config, od_context_t **out_ctx);
od_status_t od_feed_rtcm(od_context_t *ctx, const uint8_t *data,
                         size_t length, od_feed_info_t *out_info);
od_status_t od_feed_nmea(od_context_t *ctx, const char *data,
                         size_t length, od_feed_info_t *out_info);
od_status_t od_get_orbit(od_context_t *ctx, od_j2000_state_t *out_state);
od_status_t od_reset(od_context_t *ctx);
void od_destroy(od_context_t *ctx);
```

数据不足 `fit_degree + 1` 点时，`od_get_orbit` 返回 `OD_NOT_READY`。输出接口只返回最新快照；一次批量输入产生的中间历元不会另行排队。
