# 纯 C 单文件版验证记录

日期：2026-09-07；版本：1.1.0。

## 后续更新：移除平台条件编译（2026-09-08）

- 正式源码及两套源码分发包已移除 WIN32 和 OD_EMBEDDED 条件编译，统一保留原 FreeRTOS / 裸机行为；无需配置平台宏。CMake 同步移除相关选项、编译定义及 Windows 系统库链接。
- 默认构建预处理后的完整代码与修改前 OD_EMBEDDED=1 分支逐字一致，公开 API、算法及默认 30 点三次拟合配置未改变。历史记录中的嵌入式宏配置不再需要。
- CMake Release 构建成功，API / 核心 CTest 2/2 通过，现有 embedded_smoke 测试通过。严格 C99 编译屏蔽系统头文件后通过，目标文件无目录扫描、pthread、系统时间、休眠、mkdir、system 或 select 符号依赖。显式开启独立目录扫描选项的语法检查通过。
- 修改前后 NMEA 2859 点、完整 RTCM 1083 点、短 RTCM 28 点的输出 CSV 和接受/拒绝数量完全一致；两份 RTCM 的分片/reset 不变性测试通过。详细结果：`build-portable/verification.json`，修改前源码备份：`build-portable/orbit_determination.before.c`。
- 已同步更新 Linux 静态库及源码分发包；未进行目标板交叉编译或实板验证。旧 build-* 工作区和历史备份保留原样，不属于正式源码。

## 后续更新：FreeRTOS / 裸机编译模式（2026-09-07）

- 后续进一步将 `pthread.h`、pthread 类型和调用从正式源码中完全移除，不再只依赖 `OD_EMBEDDED` 条件编译；CMake 安装包也不再查找或导出 Threads 依赖。Linux 默认构建、嵌入式构建、API/核心测试和 1083 点真实 RTCM 回放均再次通过。
- 目录扫描默认关闭，不依赖 `dirent.h`。硬件工程给实现文件定义 `OD_EMBEDDED=1` 时，进一步移除 pthread、POSIX 时间、休眠、shell、目录创建与 RTKLIB 文件跟踪依赖；保留标准 C 运行库要求。默认仍为 30 点三次拟合。
- 主机上采用严格 C99、`OD_EMBEDDED=1`、隐式函数声明作为错误，并用报错头文件屏蔽 dirent、pthread、sys/select、sys/time、sys/stat，完整实现编译成功。目标文件未解析符号检查确认无目录扫描、pthread、系统时间、休眠、mkdir、system 或 select 引用。
- 嵌入式模式下，不链接 pthread，API、数值核心和真实 RTCM 分片/reset 测试通过；真实 RTCM 接受 1083 个位置点。
- Linux 默认模式 CTest 2/2 通过；桌面显式开启目录扫描分支的语法检查通过。`OD_EMBEDDED=ON`、禁用示例及测试的 CMake 静态库构建成功，未查找 Threads。
- 默认参数下，NMEA 原始日志（2859 点）、RTCM 完整日志（1083 点）和 RTCM 30 秒日志（28 点），嵌入式模式与桌面模式的接受/拒绝数量和最终状态 CSV 完全相同。记录见 `build-embedded/replay-verification.json`。
- 新增 `tests/embedded_smoke.c`：只调用公开 API、无文件 I/O，在默认 30 点三次配置下输入 4 个 NMEA 位置后成功输出。以函数/数据分段及链接垃圾回收构建后，最终程序无 dirent、pthread、系统时间、文件 I/O 或 shell 未解析符号。
- 通用源码交付包位于 `dist/freertos-generic`，包含与主源码逐字一致的 `.c`、`.h` 和 FreeRTOS 集成说明。
- Linux 交付库及其源码副本已同步更新。上述嵌入式验证使用主机 GCC，并非目标芯片交叉编译或 FreeRTOS 实板验证；板卡需使用自身标准 C 运行库、堆配置和交叉编译器编译源码。

## 后续更新：默认 30 点、三次拟合（2026-09-07）

- `od_default_config()` 和 `od_create(NULL, ...)` 改为 30 点、三次拟合；第 4 个有效位置观测开始输出，满 30 点后滚动淘汰。NMEA 和 RTCM 共用此默认配置。
- 头文件、README、接口说明和回放示例同步更新。已有三点二次拟合回归测试显式指定阶数 2，保留原覆盖范围。
- 新增默认配置行为检查：NULL/显式默认配置输出一致、第四点就绪、30 点缓存淘汰、reset 后仍按第四点就绪。
- Release 构建成功；`od_api_test`、`od_core_test` 2/2 通过。本次默认配置更新未重新运行真实 RTCM 测试。
- 已重新安装 `dist/linux-x86_64` 的静态库、头文件和单文件源码；独立预编译库消费者构建成功。
- 使用 20260718 接收机日志截至真值重叠末历元的原始字节回放，源码构建和预编译库消费者均不传容量参数，最终输出与前次显式 30 点三次拟合结果在 CSV 输出精度内一致。证据：`build-nmea-accuracy/default30-verification.json`。
- 前次精度报告 `build-nmea-accuracy/20260718/精度测试报告.md` 是修改前的历史评估，其中“默认”指当时的 3 点二次拟合；本次默认参数对应该报告的 `raw_n30_d3` 行。EOP 数据未变更。

以下为此次默认配置更新之前的单文件转换验证记录。

## 本次范围

仅验证 Linux x86-64。按用户要求，本次未构建、未运行、未验证 RISC-V。

库实现统一为 `src/orbit_determination.c`，包含数学拟合、坐标转换、EOP 表、NMEA 输入处理及必要 RTKLIB 实现。对外头文件仍为 `include/orbit_determination.h`。正式源代码、测试、示例及 CMake 项目均不需要 C++ 编译器。

## 构建和接口测试

- 使用 GNU C 15.2.0，以 `-std=c99` 直接编译和链接单文件实现及 C 回放示例成功。
- CMake `LANGUAGES C`，Release 编译成功。
- CTest 3/3 通过：`od_api_test`、`od_core_test`、`od_rtcm_capture`。
- 覆盖 NMEA 分片、缺日期、校验失败、跨午夜、重复/倒序位置点、reset、输入来源切换、未就绪输出保持、3/5/10 点环形缓存覆盖、已知二次轨迹拟合及禁止未来查询。
- 真实 RTCM 分别按 1、257、1024、最多 1048576 字节分片回放，接受数量和最终输出一致，reset 后可以重复运行。
- `ldd` 检查直接/库链接后的 Linux 回放程序，只依赖系统 C 运行库和数学库，没有 `libstdc++`。

## 与转换前版本的回归

对转换前 C++ 包装版本与新纯 C Release 程序，回放以下文件，每个文件分别使用 3、5、10 点缓存，共 9 组。

| 输入 | 每次接受位置点 | 每次拒绝记录 |
|---|---:|---:|
| `PredictOrbit/out/live_pipeline_30s.cleaned.rtcm3` | 28 | 0 |
| `PredictOrbit/out/moba_capture.rtcm3` | 1083 | 0 |
| `NMEA/270km.log` | 5701 | 0 |

各组最终历元、接受/拒绝数量均一致。最终位置和速度的 CSV 数值在输出的 9 位小数精度下一致。此检查针对上述输入和最终状态，不声称所有可能数据都能逐位一致。详细结果保存在 `build-single-c/regression.json`。

## 预编译库独立链接

新的 Linux 安装包位于 `dist/linux-x86_64`。`examples/prebuilt` 作为独立纯 C 项目，仅引用安装包头文件和 `libod.a`，不重新编译算法实现；编译链接成功，回放 30 秒 RTCM 文件接受 28 个位置点并输出 J2000 状态。

安装包同时提供单文件源码，外部项目可选择源码编译或预编译库链接。

## 旧文件处理与验证边界

转换前源码、安装包已保存在 `build-single-c/previous_sources.zip`；旧多文件实现和旧 RISC-V 安装包也移入 `build-single-c/previous-files`，不参与当前构建或安装。

本次没有进行实板测试或独立真值位置/速度精度评估。坐标模型和 EOP 表仍沿用原算法约定，详见 README。
